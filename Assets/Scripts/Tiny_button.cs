using UnityEngine;

public class Tiny_button : MonoBehaviour
{
    public float id;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
